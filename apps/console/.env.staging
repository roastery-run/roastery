# No VITE_API_URL: the console Worker serves the API on this same origin, which
# is what removes a CORS preflight from every request. An explicit value here
# would send the SPA cross-origin again.
VITE_WEB_URL=https://staging.roastery.run
